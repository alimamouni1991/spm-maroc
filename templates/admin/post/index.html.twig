{% extends 'base.html.twig' %}

{% block title %}Administration des annonces{% endblock %}

{% block body %}
    {% if is_granted('ROLE_USER') %}
    <div class="container-fluid">
        <br>
        <br>
        <br>
        <h1 class="my-5">Gestion des annonces</h1>

        <table class="table table-hover">
            <thead>
            <tr>
                <th>Id</th>

                <th>Content</th>
              {#  <th class="text-center">Réservation</th>
                <th class="text-center">Note</th>#}
              <th width="10%">Actions</th>
           </tr>
            </thead>


            <tbody>
            {% for post in posts%}
                <tr>
                    <td>{{post.id}}</td>
                    <td>{{post.content}}</td>
                  {#  <td class="text-center"> <span class="badge badge-primary rounded">{{ad.bookings | length}}</span></td>
                    <td class="text-center"> <span class="badge badge-primary rounded">{{ad.avgRatings | number_format(1, ',', ' ')}} </span></td>
                  #}  <td>
                       <div style="float: right;" >  <a href="{{ path('admin_posts_edit', {'id': post.id}) }}" class="text-primary btn text-right" >Modifier</a></div>
                        {# <a href="{{ path('post_delete' , {'id': post.id})}}" class="btn btn-danger"><i class="fas fa-trash"></i></a> #}
                {% include "reseaus/delete_form.html.twig" %} 
                   </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        {% endif %}
    </div>
 

{% endblock %}
